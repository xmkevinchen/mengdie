---
title: "Re-review: F-005 fixup commits (third-pass adhoc)"
type: review
created: 2026-05-09T19:48:21
target: "17a6a2d..HEAD (commit-range, ad-hoc)"
mode: adhoc
reviewers: [challenger, codex-proxy, gemini-proxy]
---

# Third-pass adhoc review — F-005 fixup commits `17a6a2d..HEAD`

**Mode**: ad-hoc Form 2 (commit-range review). No `verdict` field — F-005 already shipped (`status: done` at `.ae/features/done/F-005-…/`); this third pass reviews the fixup-cycle commits themselves, not the feature.

**Range reviewed**: `17a6a2d..HEAD` — 8 commits (6 at review-spawn time + 2 fixup commits landed during this third pass: `6922579` + this review record).

**Reviewers spawned**: `challenger`, `codex-proxy` (Codex/OpenAI), `gemini-proxy` → oMLX `gemma-4-26b-a4b-it-4bit` (Gemini fallback per CLAUDE.md). Default selection table skipped per ad-hoc Form 2 + no plan to anchor against. Architecture and security/performance angles deliberately not re-run (covered in first + second pass).

## Why a third pass

The user invoked `/ae:review` for the third time in the same session after F-005 had already gone through two complete review cycles (first pass with verdict `pass` + adhoc rerun with `--reviewer override`). The user explicitly chose "Review the recent fixup commits" and scope `17a6a2d..HEAD` (six commits covering both review cycles' fixups + adhoc review record). The expectation: did the fixup cycle itself ship correctly, or did the meta-process introduce its own latent issues that two prior reviews would not naturally catch?

## TL synthesis

Three review tracks executed:

- **challenger**: 5 structured challenges. #1 source-comment internal-ID residue (scope-undocumented). #2 `degraded` hint prescriptive-leak still present after 0536cb3 (overlap with Codex 2nd-pass original concern). #3 BL-034 first trigger (TTL ships) is self-referential — depends on a feature that has no BL. #4 BL-031 hint-vs-body internal contradiction (overlap with Codex 3rd-pass). #5 review record's "FIXED" marker for #2 was incomplete.
- **Codex** (cross-family, OpenAI): 2 P2 findings. (a) BL-031 internal incoherence — body endorses option (4) as cheap path, "Hint at fix shape" still tells implementer "Pick option (2)". (b) Adhoc filename / timestamp contract — flagged as P2 but on inspection mostly conflated with `manual` staging-file format; the actual SKILL example for adhoc reviews matches the file as-shipped.
- **oMLX gemma-4** (cross-family, Google angle): no P1. Confirmed BL trigger taxonomy is well-formed (≠ defensive paperwork). Independently flagged the cli.rs hint vs BL-033 design mismatch ("manually clear" prescriptive language). Noted the adhoc record could feel like a "paper trail loop" — opinion, not contract violation; SKILL explicitly defines the adhoc path.

### Disagreement Value Assessment

- **Codex's (b)** (adhoc filename contract violation) **vs. SKILL §Output case (c)**: real disagreement, resolved against Codex on close reading. Codex conflated two separate timestamp formats: the manual-mode staging-file format (`YYYYMMDDTHHMMSSsssZ`, used for `.staging-manual-...` short-lived staging files only) vs. the adhoc review-record format (`YYYY-MM-DDTHH-MM-SS` example shown in SKILL line 459). The shipped file `F-005-rerun-2026-05-09T17-06-25.md` matches the SKILL example shape. Codex partially-right on multi-reviewer naming ambiguity (the SKILL example shows single reviewer in the filename slot; multi-reviewer is unspecified) — this is at most a P3 ambiguity in the SKILL itself, not a violation.
- **challenger #1 (source-comment internal-ID residue)**: principle-application question. The 0536cb3 commit cleaned up `F-005 challenger #6 / BL-033` from operator-facing CLI text but left `F-005 challenger #5` references in source comments at `cli.rs:700` and `cli.rs:783`. challenger argues the principle is unclearly scoped; first/second-pass reviews didn't write down the scope distinction. Resolution: source comments naming the review cycle that motivated the change ARE legitimate engineering audit trail (see `git blame` + commit-message-as-truth pattern); the principle that should NOT leak is operator-facing runtime output, where the operator cannot resolve the IDs. Document this scope distinction in this review record (above) — not in the SKILL (out-of-scope here).
- **challenger #2 / Codex's repeat 2nd-pass concern / Gemma's BL-033-vs-cli mismatch**: triple-independent consensus. The second-pass review record marked Codex's original concern (`without prescribing direct DB editing in the main hint`) as `FIXED` in commit `0536cb3`, but `0536cb3` only stripped the parenthetical IDs — the prescriptive `manually clear the metrics-table row` text was preserved. Three independent reviewers (Codex 2nd-pass + Gemma 3rd-pass + challenger #2 3rd-pass) all flagged the same residue. Strong signal that the second-pass synthesis was incomplete. Fixed in commit `6922579` of this third pass.

### Findings dispositioned

| # | Source | Severity | Disposition | Where |
|---|---|---|---|---|
| 1 | Codex (a) + challenger #4 (double-flag) | P2-doc-coherence | FIXED | `6922579` (BL-031 "Hint at fix shape" updated to lead with option 4; options 1-3 documented as alternatives) |
| 2 | Codex 2nd-pass original + Gemma 3rd-pass + challenger #2 (triple-flag) | P2-prescriptive-leak | FIXED | `6922579` (cli.rs `degraded` hint rewritten to describe state, not prescribe fix shape; BL-033 body updated to reflect new hint text) |
| 3 | challenger #1 | P3-principle-scope | DOCUMENTED-HERE | source-comment internal-ID retention IS the right call (engineering audit trail); operator-facing-only is the principle. This review record memorializes the scope. |
| 4 | challenger #3 | P3-trigger-redundancy | NO-ACTION | BL-034's two triggers are not equally reliable, but the second (pre-v1 tag cut) is the real backstop. The first (TTL ships) is a nice-to-have early signal. WAIVE. |
| 5 | challenger #5 | meta | ACKNOWLEDGED | second-pass record's `FIXED` marker was incomplete on the prescriptive-leak axis; this record corrects the audit trail without modifying the second-pass record (which is immutable history). |
| 6 | Codex (b) | P3-naming-ambiguity | NO-ACTION | adhoc filename matches SKILL shape; multi-reviewer-in-filename is SKILL ambiguity, not a violation. |
| 7 | Gemma "recursion smell" | P3-aesthetic | NO-ACTION | SKILL explicitly defines adhoc record path; "looks like recursion" is opinion. |

## Fixups applied during third pass

- `6922579` — (a) BL-031 "Hint at fix shape" rewritten to lead with option (4) cheap path; options (1)-(3) preserved as alternatives. (b) `cli.rs` `degraded` hint rewritten to describe the cumulative-counter state without prescribing the manual-DELETE fix shape. Code: 7-line edit. Doc: 23 net lines added in BL-031. Verified: cargo test --test audit_stats green; clippy clean; smoke-tested the new hint via metrics-table seed.
- BL-033 body updated in this third-pass commit cycle to reference the new hint text (post-`6922579`) and to record the three-step hint-text history (4b9ac46 → 0536cb3 → 6922579) so future readers see the trajectory.

No fixup loop. No P1 escapes.

## Outcome statistics (third-pass-specific)

```
- Re-review tracks executed: 3 (challenger, codex, gemini-fallback)
- New P1 escapes: 0 (target: 0)
- New P2 findings: 2 (both FIXED inline 6922579)
  - 1 of which (#2) was the same as a 2nd-pass finding marked FIXED but actually only partial-FIX
- New P3 observations: 5 (1 documented in this record; 4 no-action)
- Cross-family complete: 100% (Codex + oMLX gemma-4 fallback both succeeded)
- Default-table skip: confirmed (security + performance not re-run per ad-hoc Form 2)
- Meta-finding: 2nd-pass synthesis missed the partial-fix axis on a finding
```

## What this third pass surfaced that two prior passes missed

1. **Two-step incomplete fix on the same finding**. Codex's 2nd-pass concern about the `degraded` hint had two parts: (i) internal tracking IDs leak into operator-facing output, (ii) prescriptive language pre-commits to a manual-fix shape. Commit `0536cb3` addressed (i) only; the synthesis recorded `FIXED` without distinguishing the two parts. Three independent third-pass reviewers re-flagged (ii) before any of them re-read each other's findings — the strongest possible signal that the partial-fix slipped through synthesis. Now FIXED in `6922579`.

2. **BL body internal incoherence after multi-pass updates**. BL-031 was updated twice. The "Hint at fix shape" section (written first-pass) and the "Why still deferred" section (updated second-pass with option 4 enumeration) drifted apart. The second-pass review's TL synthesis didn't re-read the BL bottom-up after applying the option-4 update. Fixed in `6922579`.

3. **Principle scope was unwritten**. The "no internal tracking IDs in operator-facing output" principle was applied in commit `0536cb3` to runtime CLI output but left source-code comments untouched. The scope distinction (operator surface vs engineering audit trail) was correct but never written down — challenger flagged the inconsistency. Documented in this third-pass record.

The third pass cost: ~3 reviewers × short turnaround (single-track + 2 cross-family proxies via existing MCP infra). Output: 1 partial-fix corrected to full-fix + 1 BL incoherence resolved + 1 principle scope documented. Not strictly necessary if the second-pass synthesis had been more thorough, but a cheap insurance against synthesis blind spots when iterative review cycles compound.

## Knowledge capture

(See "Knowledge Capture" section below for items ingested to Mengdie.)

## Recommendation

`status: done` for F-005 unchanged. The two third-pass fixups (`6922579` + the BL-033 update committed alongside) are correctness improvements to artifacts that already shipped; they do NOT re-issue or modify F-005's pipeline-state verdict. F-005 remains archived at `.ae/features/done/F-005-…/`.

Future review-cycle hygiene: when synthesizing a multi-axis finding (e.g., "ID leak + prescriptive language"), the synthesis MUST enumerate each axis and check the fixup against each one independently. A single `FIXED` marker on a multi-axis finding is the failure mode this third pass corrected.
