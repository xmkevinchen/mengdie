---
title: "Re-review: F-005 mengdie audit-stats subcommand"
type: review
created: 2026-05-09T17:06:25
target: ".ae/features/done/F-005-mengdie-audit-stats-doctor-subcommand-fo/plan.md (rerun)"
mode: adhoc
reviewers: [architecture-reviewer, challenger, codex-proxy, gemini-proxy]
---

# Re-review: F-005 mengdie audit-stats subcommand

**Mode**: ad-hoc rerun (user invoked `/ae:review --reviewer override` with all four selected). No `verdict` field — pipeline state is already `done` from the first-pass review.

**Range reviewed**: `ac09f9e..HEAD` — 8 commits total at second-pass time (4 implementation + 2 first-pass review fixups + 2 second-pass fixups landed during this rerun).

**Reviewers spawned**: `architecture-reviewer`, `challenger`, `codex-proxy` (Codex/OpenAI), `gemini-proxy` (oMLX gemma-4-26b-a4b-it-4bit fallback per CLAUDE.md cross-family policy). Default selection table skipped per `--reviewer` override semantics. Security and performance tracks deliberately not re-run.

## Why a second pass

The first-pass review (`/ae:work` cycle on 2026-05-09 → `.ae/features/done/F-005-…/review.md`, verdict pass) closed F-005 cleanly with two fixup commits (`4b9ac46`, `e0f2615`) and three BL items (BL-031 update + BL-032 + BL-033). The user invoked `/ae:review` again immediately after, and explicitly chose `--reviewer override` with all four selected reviewers. The expectation is fresh-eyes-on-the-fixups: did the first-pass synthesis itself have blind spots? Did the fixup commits introduce new issues?

## TL synthesis

Four review tracks executed:

- **architecture-reviewer**: PASS. Two new P2 findings (#3 `not_yet_triggered` semantic hazard, #4 cmd_list backport debt) + four P3 observations (AuditStats visibility, missing Debug derives, BL-033 hint detail, supersession_count_30d join-pair-vs-distinct-fact ambiguity). Confirms first-pass `Copy + Clone` fixup is correct and BL-032/BL-033 triggers are well-formed.
- **challenger**: 4 second-pass items. #A (hint internal-ID leak) — independently flagged at P2-UX. #B (cmd_list inconsistency, overlap with architecture #4) — P3-architecture. #C (audit-stats naming aging) — confirms first-pass WAIVE. #D (failure-path JSON contract) — confirms first-pass advisory PASS.
- **Codex** (cross-family, OpenAI): no P1. Two specific findings: (1) `Copy + Clone` on `AuditStatus` is correct, no subtle-copy risk; (2) hint embeds `F-005 challenger #6 / BL-033` — internal tracking ID leak in operator-facing CLI output (P2-UX).
- **oMLX gemma-4** (cross-family, Google angle, Gemini fallback): no P1. Independently flagged the hint internal-ID leak as "leaky abstraction" violating separation-of-concerns. Confirmed JSON wire contract is additive-friendly. Recommended documentation of `degraded` state thresholds for downstream consumers.

### Disagreement Value Assessment

- **first-pass `not_yet_triggered` waive vs architecture #3 (this rerun) "semantic hazard"**: not really disagreement — both agree the v0.0.1 disposition (waive) is correct. The novelty is that architecture #3 names a concrete hazard (TTL-rotation-induced false-`not_yet_triggered`) that first-pass synthesis had not articulated. The waive holds, but the rename trigger is now bound to "audit-table TTL/rotation ships OR v1 tag cut, whichever first" rather than the looser "reversible pre-v1".
- **first-pass synthesis's silence on `cmd_list` debt vs architecture #4 + challenger #B (this rerun)**: real first-pass blind spot. The plan body explicitly cited `cmd_list`'s silent-fallthrough as the *motivation* for F-005's typed enum, but the first-pass review missed that the fix was applied only to `cmd_audit_stats` and not back-ported. BL-035 (filed in commit `f1aae36`) closes the gap.
- **3-of-4 reviewers (Codex + Gemma + challenger #A) vs the originating commit author (me, in `4b9ac46`)** on hint internal-ID leak: strong consensus against the author's choice. Fixed in `0536cb3`.

## Findings dispositioned (severity / action)

| # | Source | Severity | Disposition | Where |
|---|---|---|---|---|
| 1 | Codex + Gemma + challenger #A (3-way consensus) | P2-UX | FIXED | `0536cb3` (hint stripped of `F-005 challenger #6 / BL-033`; actionable guidance preserved) |
| 2 | architecture #3 | P2-semantic | BACKLOG | BL-034 (rename trigger: audit-table TTL/rotation ships OR pre-v1 tag cut) |
| 3 | architecture #4 + challenger #B (overlap) | P2-consistency | BACKLOG | BL-035 (cmd_list ValueEnum back-port; trigger: next cmd_list touch OR third format-bearing subcommand) |
| 4 | challenger #C | P3-naming-aging | NO-ACTION | confirms first-pass waive that `audit-stats` ages well at current scope |
| 5 | challenger #D | n/a | CONFIRMED-PASS | failure-path JSON contract is honored (DB lock / query failure bubbles up before any stdout write; AuditStatsOutput types cannot fail to serialize) |
| 6 | architecture #1 | P3-structural | NO-ACTION | `AuditStats` `pub` matches `DbStats` pre-existing pattern; no external consumer |
| 7 | architecture #2 | P3-consistency | NO-ACTION | missing `Debug` derives on AuditStats / DbStats / AuditStatus — pre-existing pattern |
| 8 | architecture #5 (BL-033 hint detail) | P3-doc | NO-ACTION | minor — `DELETE` vs `UPDATE … = 0` is correct but unexplained in BL-033 body |
| 9 | architecture #6 | P3-naming | NO-ACTION | `supersession_count_30d` counts join pairs not distinct facts; comment explains, field name does not — note for A-MEM trigger design phase |

## Fixups applied during rerun

- `0536cb3` — strip `F-005 challenger #6 / BL-033` from `degraded` hint text. 3-line change. Tests green; clippy clean; smoke-tested via metrics-table seed.
- `f1aae36` — file BL-034 + BL-035, update BL-033 to point at the new commit. Doc-only; pre-commit hooks pass.

No fixup loop. Cumulative effect of the rerun: 1 P2 fixed inline + 2 new P2 backlogged + 1 existing BL refreshed.

## Backlog snapshot after rerun

| BL | Trigger | Source |
|---|---|---|
| BL-031 (existing, updated 2x) | Operator alerts on `supersession_count_30d` OR SQL refactor proposed | F-005 Step 4 accumulated checkpoint + cross-family Codex (option 4 added) |
| BL-032 (existing) | `memory_search_audit` >10k rows OR `mengdie audit-stats` >20ms | First-pass perf reviewer P2 |
| BL-033 (existing, updated for accuracy) | First real fix-and-reset cycle in production | First-pass challenger #6; second-pass cleanup of internal-ID leak |
| BL-034 (new this rerun) | Audit-table TTL/rotation ships OR pre-v1 tag cut | Second-pass architecture #3 |
| BL-035 (new this rerun) | Next `cmd_list` touch OR third `--format`-bearing subcommand | Second-pass architecture #4 + challenger #B |

All five BLs are `defer-until-trigger`. None blocks F-005 ship; F-005 remains `status: done` and archived.

## Outcome statistics (rerun-specific)

```
- Re-review tracks executed: 4 (architecture, challenger, codex, gemini-fallback)
- New P1 escapes: 0 (target: 0)
- New P2 findings: 3 (1 fixed inline 0536cb3, 2 backlogged BL-034 + BL-035)
- New P3 observations: 6 (all no-action)
- Confirmation of first-pass waivers: 2 (challenger #C audit-stats naming, challenger #D failure-path)
- Cross-family complete: 100% (Codex + oMLX gemma-4 fallback both succeeded)
- Default-table skip: confirmed (security + performance not re-run per --reviewer override semantics)
```

## Knowledge capture

(See "Knowledge Capture" section below for items ingested to Mengdie.)

## Why this rerun added value

Three concrete things the second pass surfaced that the first pass did not:

1. **Internal-ID leak in operator-facing CLI output** (3-way consensus). The first-pass author (me) introduced this in `4b9ac46` and didn't notice. The second-pass review caught it. Fixed.
2. **`not_yet_triggered` semantic hazard tied to TTL rotation** (architecture #3). The first-pass disposition ("waive as forward-looking") was directionally correct but didn't name the concrete future scenario that would invalidate the label. BL-034 now binds the rename to that scenario explicitly.
3. **`cmd_list` backport debt is documented but untracked** (architecture #4 + challenger #B). The first-pass review missed that the F-005 plan body cited `cmd_list`'s silent-fallthrough as the motivation but didn't fix it. BL-035 closes the gap.

The rerun did NOT change the core verdict — F-005's implementation is correct, ACs satisfied, no P1 escapes. But the second pass found three real issues (one fixable, two BL-trackable) that the single-pass review had missed. The cost (~3 reviewers × ~15min) is paid back by the catch.
