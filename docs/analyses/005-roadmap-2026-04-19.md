---
id: "005"
title: "Roadmap snapshot: mengdie backlog after plan 012"
type: retrospect
created: 2026-04-19
data_sources: 15 backlog items + 19 discussions + 12 plans
---

# Roadmap: mengdie (2026-04-19)

## Layout Note

mengdie uses a **legacy flat `docs/backlog/` layout** — no `v*/` sprint
directories, no `.ae/roadmaps/` sprint docs. This matches the project's
solo-dev, single-cadence workflow (CLAUDE.md "Project Status" section
acts as the informal roadmap). No migration warranted at this scale;
the v2 sprint infrastructure is AE-plugin-specific tooling.

Output below uses the legacy cluster view only.

## Pipeline State

- **Active discussions**: 0 in-progress (all 19 discussions have concluded
  or are deferred with findings captured in backlog)
- **Draft plans**: 0 (plan 012 is the last; now `status: done`)
- **Reviewed plans awaiting work**: 0
- **Work-in-progress plans**: 0
- **Plans complete, awaiting review**: 0
- **Open backlog items**: 14 (15 files; 1 closed mid-session:
  BL-synthesis-cli-skip-metric)

## Theme: Synthesis quality + observability (6 items, 1 just-closed)

Active intelligence-layer instrumentation. Dominant cluster.

- [closed] BL-synthesis-cli-skip-metric — shipped by plan 012 (2026-04-19)
- [closed] BL-synthesis-result-struct-promotion — superseded by plan 012
- [open]   BL-clustering-validation — AC5 filled in 2026-04-19; second trigger
           still armed ("residuals > 50% AND synthesis_hit_rate < 10%")
- [open]   BL-synthesis-preload-db-miss-edge — filed 2026-04-19; theoretical
           edge (denominator inflation on DB miss); trigger = first observed
           mismatch or concurrent-delete path
- [open]   BL-synthesis-dedup-key — content_hash stability for synthesis
           retries; cross-family finding from BL-007 review
- [open]   BL-synthesis-provenance — synthesis→source linkage quality,
           from BL-007 review challenger

**Dependency**: The 4 open items share the dream-synthesis pipeline. Next
dream run (or measurement sprint) could address them together. BL-clustering-
validation is the observability gate for the other three.

## Theme: Schema + DB hygiene (2 items)

- [open] BL-fk-pragma-and-deletion-safety — FK cascade behavior on memory delete
- [open] BL-valid-until-boundary — temporal invalidation edge at exact-match timestamps

**Pairing**: both require DB schema work; bundlable if a migration plan
lands.

## Theme: Refactor + module organization (1 item)

- [open] BL-dreaming-module-split — dreaming.rs at 890+ lines; candidate
  for split into dreaming/mod.rs + synthesis_pass.rs + tests module

**Trigger**: add a second synthesis-variant caller (e.g., re-synthesis
pass, back-pressure throttle). Single-caller state keeps the module
cohesive enough for now.

## Theme: CI (1 item)

- [open] BL-ci-full-clippy-test — Forgejo CI runner environment fix; currently
  pinned to cargo fmt only. Trigger: Forgejo runner gets a clean bash env OR a
  hosted GitHub Actions runner is added.

## Theme: Legacy review rollups (4 items, low signal)

- 001-qmd-learnings — 2026-04-05 review leftovers from plans 001/002
- 002-review-deferred — plan 002 deferred findings
- 003-review-p3 — plan 003 P3s
- 004-analyze-findings — mengdie project analyze-era findings (2026-04-05)

These are BEFORE the project adopted the "trigger-annotated BL" format.
Suggest a one-time sweep to either: (a) promote still-relevant items to
BL-* naming + triggers, or (b) close as obsolete. Most predate plan 007;
many are likely superseded by subsequent work.

## Theme: Phase meta (1 document)

- 005-phase2-roadmap — informal Phase 2 roadmap from 2026-04-16. Now
  largely executed: plans 007, 009, 010, 011, 012 delivered the
  intelligence layer primitives. Could be closed as "fulfilled" or
  refreshed into a Phase 3 scoping doc.

## WIP Flow Status

- In Progress: 0 plans
- Review: 0 plans
- Stalled: 0 items (no active work > 7 days idle)

**Verdict**: clean flow. No WIP overload, no stalled items. Natural
break-point for triage or rest.

## Release-Readiness (informal — no .ae/roadmaps/)

mengdie has no explicit version cadence. Inferred shipping points:
- **Phase 1 (MVP)**: complete at plan 005 (project naming)
- **Phase 2 (intelligence layer)**: complete at plan 012
- **Phase 3 (?)**: not yet defined

The 14 open BLs are all post-Phase-2 refinement / theoretical edges, no
ship-blocking items. A "Phase 2 done" tag commit would be defensible.

## Version Suggestion

Three candidate groupings for the next cadence unit (plan-cycle or
informal "v0.x"):

### Option A: Synthesis measurement sprint (4 BLs)
Address synthesis quality empirically. Pair BL-clustering-validation,
BL-synthesis-preload-db-miss-edge, BL-synthesis-dedup-key,
BL-synthesis-provenance. Single dream-run gives signal for all four.
Size: M-L. Prerequisite: nothing.

### Option B: DB schema hardening (2 BLs)
Bundle BL-fk-pragma-and-deletion-safety + BL-valid-until-boundary into
one migration plan. Size: S. Prerequisite: none.

### Option C: Legacy backlog sweep (5 items)
One-time housekeeping: triage the 4 pre-BL-convention rollup docs +
005-phase2-roadmap. Each item: close as obsolete OR rewrite into BL-*
with trigger. Size: S. Prerequisite: none. Highest ratio of backlog
shrinkage per effort.

## Recommendation

Run **Option C first** (15 minutes, cuts 5 items), then pick between
A and B based on which has the clearer immediate signal. Option A is
higher-value (unlocks the null-escape-hatch tuning loop); Option B is
faster and more atomic. Don't bundle A + B — they share no files.

## Key Dependencies

- BL-clustering-validation is the measurement gate for 3 other synthesis
  BLs — if it advances, the others' triggers fire on the same data.
- BL-ci-full-clippy-test depends on external infra (Forgejo runner fix);
  not actionable until that unblocks.
- No BL is blocked by another BL via explicit `blocked_by:` (flat
  dependency graph).
