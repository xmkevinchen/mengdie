# v0.0.1 — minimum-viable AE-brain

**Released**: 2026-05-10
**Branch**: `main` (post-merge of `feature/v0.0.1-rebuild`)
**Tag**: `v0.0.1` → `463c2f4`
**Pre-release**: yes — semver 0.x signals alpha; operator scope is single-user.

---

## TL;DR

mengdie reset from v0.x branch (frozen at v0.8.0) to v0.0.1 as a **minimum-viable AE-brain that avoids re-inventing wheels**. Narrow OSS adoption, keep working in-house code (per `docs/v0.0.1-rebuild-plan.md` thesis + `docs/discussions/026-rust-oss-survey/analysis.md` 14-library scorecard).

**Cargo.toml net delta: +1 line** (`sqlite-vec = "0.1.9"`). All other adoptions ride existing transports (claude-CLI flags, in-house FTS5 / fastembed-rs / rusqlite).

## Ship contents (7 plan/feature cycles)

| Feature | Scope |
|---|---|
| **F-001** | sqlite-vec compatibility verification spike (BL-026 prereq) |
| **F-002** | Persisted domain audit + link tables (`memory_search_audit`, `audit_returned_facts`) — every `memory_search` call now leaves an audit trail |
| **F-003** | Retrieval & ingest layer consolidation (`memory_search_audited` orchestrator; `Db::search_fts` / `search_vector` downgraded to `pub(crate)` so the audit invariant cannot be bypassed) |
| **F-004** | Project doc structure overhaul |
| **F-005** | `mengdie audit-stats` CLI subcommand — reads F-002 audit tables, emits human table + script JSON via `--format`, health enum `ok` / `not_yet_triggered` / `degraded` |
| **F-006** | sqlite-vec adoption replaces `vector.rs` brute-force cosine similarity (264 LoC → vec0 vtable + triggers) |
| **Plan 019** | synthesis CLI `--json-schema` adoption (BL-027 Path B) — replaces ~30 LoC brace-depth scanner with claude-CLI's native structured-output flag |

## Highlights

### F-006: sqlite-vec adoption
- Replaces 264 LoC brute-force cosine similarity in `src/core/vector.rs`
- `sqlite-vec` v0.1.9 `vec0` virtual table + triggers
- Loads via `rusqlite` extension API at DB-open time
- fastembed-rs unit-vector invariant asserted at the embedder boundary (`assert!`, not `debug_assert!`) — the similarity formula's unit-norm assumption can never silently drift

### Plan 019: synthesis CLI --json-schema
- `LlmProvider::complete_structured` sibling trait method with default impl
- Flat-shape schema with `skip:bool` discriminator (the originally-planned `oneOf` was rejected mid-execution — Anthropic API does not accept top-level `oneOf`/`allOf`/`anyOf` in tool `input_schema`)
- Schema lives in `resources/synthesis-output-schema.json` (`include_str!` embedded)
- New error variants `StructuredOutputMissing` + `StructuredOutputWrapperInvalid` both carry `"(verify claude >= 2.1.138 supports --json-schema)"` diagnostic suffix
- **Production validation**: 5/5 syntheses, 0 errors, ~$0.40 USD-equivalent, ~275K tokens (Pro flat-fee actual: $0)

### F-005: `mengdie audit-stats` CLI
- Shipped as `audit-stats`, NOT `doctor` (working name in `docs/v0.0.1-rebuild-plan.md`) — operator convention favored aligning with the underlying audit-table feature

## Retrospective findings (recorded for next cycle)

Plan 019 final review captured three process-level lessons worth recording at the version boundary:

1. **Plan-review must run actual API probes for provider-specific schema assumptions.** The 9-reviewer plan-review for BL-027 endorsed a `oneOf` schema design that Anthropic API later rejected at runtime; no reviewer probed the actual API subset. **Citation alone is sufficient for plain object schemas; probe required for `oneOf` / `anyOf` / `allOf` / `const` / `additionalProperties:false` / conditional required / wrapper shape / error-shape assumptions.** Cost: hours of mid-execution debugging; saved by 5-min upfront probe.

2. **Reject path-out-of-scope arguments framed by phantom metered cost when the operator runs on flat-fee subscription.** Plan 019's "Out of scope" rejected Path C (direct Anthropic HTTP API) citing per-token cost — irrelevant under Claude Code Pro. Correct rejection rationale: "preserves credential-delegation privacy posture", not "$0.24 per call".

3. **Anchor BL triggers to code artifacts, not external events.** BL-039 originally specified "when second LLM provider lands" — vague. Sharpened at v0.0.1 ship to three concrete tripwires (second `build_provider` match arm / second `impl LlmProvider for X` / Cargo.toml non-claude LLM dep) plus an inline NOTE comment at the `build_provider` site itself.

Full text: `docs/reviews/019-synthesis-cli-json-schema.md` "Retrospective findings".

## Production DB state at ship

```
69 memories (67 active, 11 long-term, 46 recalled at least once)
audit health: status=ok, 20 audits, 92 result links, 0 write failures
context injection rate: 75.0%
conflict detection rate: 12.1% (4/33 ingestions)
```

## Test status

```
312 passed, 7 ignored (12 suites)
clippy clean, format clean
```

## Backlog state

13 deferred BLs filed under `docs/backlog/unscheduled/` (BL-029 ~ BL-041), all trigger-annotated. None block v0.0.1; most fire on operator scale changes or specific code-artifact events.

Key forward-looking BLs:

- **BL-039**: re-evaluate `rig::Extractor` when a second LLM provider lands (trigger: second `impl LlmProvider for X` or non-claude LLM dep in Cargo.toml)
- **BL-040**: `classify_output` drops stdout on non-zero exit — operability gap surfaced during plan 019 Step 4 debugging
- **BL-041**: WrapperEnvelope version-bump verification checklist on claude-CLI upgrade beyond 2.1.138

## Branch & artifact map

```
main             463c2f4  ← v0.0.1 ship (Cargo version synced)
                          ↳ ed9cbc4 (merge commit) ↳ bed77e8 (pre-merge ship head)
v0.8.0-frozen    8852e09  ← pre-rebuild backup (preserved on remote)
tag v0.0.1                ← 463c2f4 (Cargo version 0.0.1)
```

## Pending operator action

- Forgejo release page: this document is the release notes body.
- No CI/build pipeline integration changes shipped — pushing the tag does NOT auto-build artifacts. If artifact release is wanted, restore the `.gitea/workflows/release.yml` from `v0.8.0-frozen` lineage (was removed during the rebuild).

## Full CHANGELOG

See `CHANGELOG.md` `v0.0.1 — 2026-05-10` section.

## Final review

See `docs/reviews/019-synthesis-cli-json-schema.md` (verdict: PASS).
