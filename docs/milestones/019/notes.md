# Plan 019 — Milestone Notes

DEFERRED [Step 4]: Verify claude-CLI accepts `$schema` and `$comment` JSON-Schema-spec metadata fields in `resources/synthesis-output-schema.json`.
Reason: BL-027 verification used a schema without these fields; claude-CLI's `--json-schema` validator may reject them. If Step 4 e2e exposes rejection, strip both fields from the JSON file (high reversibility — single-file edit). If Step 4 passes, leave fields as documentation value.
Disposition: FIXED. Step 4 production probe revealed a more fundamental issue: Anthropic API rejects top-level `oneOf`/`allOf`/`anyOf` in `input_schema` ("API Error: 400 ... does not support oneOf, allOf, or anyOf at the top level"). Schema rewritten as flat object with `skip:bool` discriminator (Step 4 commit). `$schema` and `$comment` fields were dropped in the same edit — Anthropic's input_schema subset doesn't formally accept them either, and the working schema in BL-027 verification didn't carry them. See `docs/spikes/019-rate-limit-measurement.md` "Schema-shape post-mortem" for the full incident write-up.
